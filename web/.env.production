# Committed on purpose: this is a public API URL, not a secret, and having it
# in the repository makes the Vercel build reproducible without console config.
NEXT_PUBLIC_API_BASE=https://warmline-api.mziyo.com
